spring:
  application:
    name: studentAdmission

  datasource:
    url: jdbc:mysql://localhost:3306/studentdb
    username: root
    password: sangita@2845

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
